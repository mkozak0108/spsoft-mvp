# Address of the viewer (apps/viewer) that the scoring app embeds on the left.
# Not a secret: it ends up in the browser bundle, like every VITE_ variable.
# Defaults to http://localhost:3000 when unset. Its origin is also the only
# origin the scoring app accepts bridge messages from.
VITE_VIEWER_URL=http://localhost:3000
